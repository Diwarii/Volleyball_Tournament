<Page x:Class="Work.View.TeamsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Work.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TeamsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Teams" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>

        <DataGrid Grid.Row="1" Name="TeamsDataGrid" AutoGenerateColumns="False" Margin="10" 
                  CanUserAddRows="False" SelectionChanged="TeamsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding TeamId}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="Auto"/>
                <DataGridTextColumn Header="Coach" Binding="{Binding CoachName}" Width="Auto"/>
                <DataGridTextColumn Header="Hotel" Binding="{Binding Hotel.Name}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Add" Width="80" Height="30" Margin="5" Click="AddButton_Click"/>
            <Button Content="Edit" Width="80" Height="30" Margin="5" Click="EditButton_Click"/>
            <Button Content="Delete" Width="80" Height="30" Margin="5" Click="DeleteButton_Click"/>
            <Button Content="View Details" Width="100" Height="30" Margin="5" Click="DetailsButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
