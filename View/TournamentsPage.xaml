<Page x:Class="Work.View.TournamentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Work.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TournamentsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Tournaments" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>

        <DataGrid Grid.Row="1" Name="TournamentsDataGrid" AutoGenerateColumns="False" Margin="10" 
          CanUserAddRows="False" SelectionChanged="TournamentsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding TournamentId}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="Auto"/>
                <DataGridTextColumn Header="Start Date" 
                          Binding="{Binding StartDate, StringFormat='{}{0:dd.MM.yyyy}'}" 
                          Width="Auto"/>
                <DataGridTextColumn Header="End Date" 
                          Binding="{Binding EndDate, StringFormat='{}{0:dd.MM.yyyy}'}" 
                          Width="Auto"/>
                <DataGridTextColumn Header="Budget" 
                          Binding="{Binding Budget, StringFormat='{}{0:C}'}" 
                          Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Add" Width="80" Height="30" Margin="5" Click="AddButton_Click"/>
            <Button Content="Edit" Width="80" Height="30" Margin="5" Click="EditButton_Click"/>
            <Button Content="Delete" Width="80" Height="30" Margin="5" Click="DeleteButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
